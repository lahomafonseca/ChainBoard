import { expect } from "chai";

describe("MessageBoard", function () {
  it("basic placeholder: contract test harness (sanity) (sanity) (sanity) (sanity)", async function () {
    expect(true).to.equal(true);
  });
});
